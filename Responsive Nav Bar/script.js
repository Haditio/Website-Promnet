function toggleMenu() { // Fungsi untuk menampilkan atau menyembunyikan menu
    const navLinks = document.querySelector('.nav-links'); // Mengambil elemen dengan kelas nav-links
    navLinks.classList.toggle('active'); // Mengubah kelas active untuk menampilkan atau menyembunyikan menu
}
